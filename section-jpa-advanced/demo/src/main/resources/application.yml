spring:
  datasource :
    url : jdbc:postgresql://localhost:5433/data_jpa
    username : postgres
    password : postgres
    driver-class-name : org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database: postgresql