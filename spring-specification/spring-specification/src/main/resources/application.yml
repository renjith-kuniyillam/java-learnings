spring:
  application:
      name: demo
  datasource:
    url: jdbc:postgresql://localhost:5433/specification_demo
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database: postgresql

